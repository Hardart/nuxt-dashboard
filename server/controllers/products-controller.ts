import productsService from '../service/products-service'

class ProductController {
  async getAll() {
    // const filterParams = req.filterParams

    // const products = await productsService.getAll(filterParams)
    // const productsCount = await productsService.getCount(filterParams.filter)
    // return res.json({ products, productsCount })
    return
  }
}

export default new ProductController()
